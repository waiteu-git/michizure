import { env } from 'cloudflare:test'

/**
 * 🔴 本番の速度制限をテストの都合で決めない。
 *
 * テストは同じ vitest プロセス内で部屋を何十回も作る（2026-09-18時点で約56回・
 * 今後のテスト追加で増え続ける）。ここを素通しにしないと、本番のしきい値が
 * 「テストが壊れない値」に引きずられ、テストが増えるたびに緩む方向へ
 * ラチェットしてしまう（2026-09-18の監査で指摘）。ここでテスト実行中だけ
 * 無効化し、`wrangler.toml` の値は中身（本番でどれだけ絞りたいか）だけで決める。
 */
env.ROOM_CREATE_LIMITER = { limit: async () => ({ success: true }) }
