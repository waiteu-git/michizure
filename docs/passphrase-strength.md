# 合言葉の強度と PBKDF2 反復回数 — 分析と裁定

作成日: 2026-08-10 / 裁定: 2026-08-11（生成した合言葉を既定にする＝**1,024語×5語**）
実測: 2026-08-14（Android 実機で 600,000回=70ms ⇒ **600,000 のまま据え置き**）
背景: 法務下調べ（ビジネスハブ）により、**「サーバーが `SHA-256(authKey)`＋salt を持つ構成では、同時漏えい時に弱い合言葉へのオフライン辞書攻撃が成立しうる」**ことが論点になった。設計 §16 の「PBKDF2 反復回数」を**端末性能だけで決めてはいけない**。

> ⚠ **法的評価はここでは行わない。** 「この構成なら例外に当たる」とは書かない。
> ⚠ **現時点で答えが返ってくる照会は走っていない**（照会の経路は 2026-09-10 に切り替わった（METI は不成立で終了→PPC デスク等へ・経路はユーザー裁定待ち））。「回答待ち」ではない。
> ここで出すのは、判断に必要な**技術的な事実と選択肢**だけである。

## 前提と、どこまでが実測か

| | 値 | 種別 |
|---|---|---|
| PBKDF2 600,000回（Mac M1 / **Node** の WebCrypto） | **73ms** | ✅ 実測（`scripts/bench-pbkdf2.mjs`） |
| PBKDF2 600,000回（Mac M1 / **ブラウザ**の WebCrypto） | **173ms** | ✅ 実測（`bench/pbkdf2.html` を localhost で実行） |
| PBKDF2 600,000回（**Android 実機** / Chrome 151） | **70ms** | ✅ 実測（2026-08-14・3回の中央値） |
| PBKDF2 1,000,000回（同上） | 116ms | ✅ 実測 |
| 攻撃者の GPU が CPU1コアの何倍か | **1,000〜3,000倍と仮定** | ⚠ **仮定**（測定していない。PBKDF2-HMAC-SHA256 の一般的な並列化効率からの見積もり） |

⚠ **「ブラウザは Node より遅い」は一般化できない**（2026-08-14に実機で否定された）。
Mac では確かに 173ms 対 73ms だったが、**Android 実機は 70ms** で Mac のブラウザより速い。
⇒ あの2.4倍は **Mac のブラウザという1つの環境の性質**であって、ブラウザ一般の性質ではなかった。
**測った世界の外へ持ち出さないこと。**

以下の年数は**この仮定の上での計算結果**であって、測定値ではない。桁の感覚をつかむために使う。

## 結論: 効くのは合言葉であって反復回数ではない

反復 600,000 回・攻撃者が 1,000〜3,000倍のとき、平均的な総当たり時間:

| 合言葉の作り方 | 強度 | 割られるまで |
|---|---|---|
| 4桁の数字 | 13 bit | **0.1〜0.4秒** |
| よくある単語1つ（例:「あいことば」「おきなわ」） | 17 bit | **1〜4秒** |
| ランダムな単語2つ | 25 bit | 5〜15分 |
| ランダムな単語3つ | 37 bit | 18〜53日 |
| **ランダムな単語4つ** | **49 bit** | **241〜723年** |
| ランダム英数字8文字 | 48 bit | 84〜253年 |

**反復回数を2倍にして得られるのは 1 bit。5,000語から単語を1つ足すと 12.3 bit。**
⇒ 反復回数を600,000から1,200,000へ倍にしても「よくある単語1つ」は 1〜4秒が 2〜7秒になるだけで、**守れない状態は変わらない**。

⚠ 現在テストやE2Eで使っている「たびのあいことば」「おきなわのあいことば」のような合言葉は、**この表でいう最下段に属する**。

## 方針（2026-08-11 ユーザー裁定＝案1・再litigate不要）

反復回数は「一番遅い端末が耐えられる上限」で決め、**強度は合言葉側で作る**。その作り方の選択:

### ✅ 案1: 生成した合言葉を既定にする【採用】

部屋を作るとき、**日本語の単語をランダムに組み合わせた合言葉を既定で提示**する。利用者は「自分で決める」を選べるが、その場合は最低要件を満たすまで進めない。

🔴 **語数と語彙は 2026-08-11 に確定＝1,024語のリストから5語（約50bit）。**
当初案（5,000語×4語）から変えた理由は `docs/passphrase-wordlist.md`
＝**MIT だけで使える日本語語彙が 2,048語しかなかった**ため、語彙を絞って語数を増やす側へ倒した。
下の表は「語数を増やすと何が起きるか」の桁の感覚として読むこと（語彙数が違うので値は一致しない）。

- 利点: **旅行の同行者へ口頭で伝えられる**（QRやURLと一緒に共有する場面に合う）。強度が利用者の語彙センスに依存しない
- 利点: 「合言葉を紛失すると復旧できない」という制約とも相性がよい（提示時にコピー/保存を促せる）
- 欠点: 「好きな言葉にしたい」利用者には一手間。単語リスト（5,000語程度）をアプリに同梱する必要がある（数十KB）

### ❌ 案2: 自由入力のみ＋強度メーター【不採用】

入力欄の下に強度を表示し、弱ければ警告する。止めはしない。

- 欠点: **警告は読まれない。** 実効は案3とほぼ同じになりやすい

### ❌ 案3: 現状のまま（制限なし）【不採用】

- 欠点: 上表の最下段が既定になる。**設計 §7.4 の「想定する脅威は合言葉を知る友人」という前提が、漏えい時の議論では通らない**

## 反復回数そのもの

実機測定（`bench/pbkdf2.html`）の結果で決める。目安:

**✅ 2026-08-14 に実機で測り、600,000 のまま据え置くと決めた。**

- Android 実機（Chrome 151）で **600,000回=70ms**、1,000,000回でも 116ms＝待ち時間としては問題にならない
- ⇒ **速いから上げる、はしない。** 倍にして得られるのは 1 bit で、合言葉が既に 50bit を出しているため釣り合わない
- ⚠ 測ったのは1台。**一番遅い端末に合わせる**という原則は生きている。ただし 70ms なら
  10倍遅い端末でも 700ms で収まるため、実用上の余裕は大きい
- ⚠ 変えても既存の部屋は壊れない（反復回数は部屋ごとに保存済み・§6）

## この判断が影響する先

- **個人情報保護法の論点 Q2**（漏えい報告義務と「高度な暗号化」）— 合言葉の強度要件はこの論点の前提に入る。
  ⚠ **法的評価は専門家の判断を待つ**。⚠ 以前は METI への照会で聞く予定だったが、照会は 2026-09-10 に
  不成立で終了した＝**この論点はまだ誰にも聞けていない**（経路はユーザー裁定待ち）
- **Phase 1b の UI**（合言葉の入力・生成・共有の導線）
- 設計 §7.4 の脅威モデルの記述（「想定する脅威は合言葉を知る友人」の但し書き）
