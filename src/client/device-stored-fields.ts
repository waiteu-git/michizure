/**
 * 🔴 **端末（ブラウザの localStorage）に保存するもの＝事実の一覧。**
 *
 * プライバシーポリシー §2.1「利用者の端末に保存するもの」の**消費元**。サーバー側
 * （src/server-stored-fields.ts）と対になる。⇒ **ここを変えたら PP §2.1 もビジネスハブと合わせて直すこと。**
 *
 * なぜ要るか（2026-09-11）: 9/6〜9/7 に、3方向マージの土台（base＝最後に双方が一致した版の**平文**）と
 * 版の番号（rev）を部屋の控えに、入口の材料（entry）を部屋の一覧に足した。どれも PP §2.1 に載らないまま
 * だった＝**消した記録が画面に出ないまま平文で端末に残る**ことも書かれていなかった
 * （2026-09-10 の PP×実装の突き合わせ監査で指摘）。サーバー側は歯止めで止まるようにしたが、
 * **端末側には歯止めが無く、同じ形が3回続いた**。
 *
 * 検査: test/device-stored-fields.test.ts が、作成・入室・圏外の記録・合流・消去を実際に通してから
 * localStorage を吐き出し、キーの形も欄もここと照合する。
 */
export const DEVICE_STORED = {
  /** 部屋の一覧（入口に出る旅行） */
  rooms: {
    key: 'michizure.rooms.v1',
    /** 配列の各要素の欄 */
    fields: ['encKey', 'entry', 'name', 'roomId', 'token'],
    /** entry の中の欄（入口の材料＝圏外で人を招く QR 用。秘密ではない） */
    entryFields: ['iterations', 'kdfVersion', 'salt'],
    説明:
      '部屋のID・接続用のトークン（30日）・**暗号を解く鍵**・旅行の名前・入口の材料（salt・反復回数・版）。' +
      '鍵は合言葉なしで中身を読むのに十分',
    PP: '§2.1「暗号を解く鍵」「接続用のトークン」「旅行の名前」（⚠ 入口の材料と部屋IDの一覧は未記載＝監査 #4）',
  },
  /** 部屋ごとの控え */
  state: {
    keyPrefix: 'michizure.state.',
    fields: ['base', 'baseStamp', 'dirty', 'rev', 'state'],
    説明:
      '今の中身（**平文**）と、同期の土台 base（最後に双方が一致した版の**平文**）・その目印・版の番号・' +
      '未送信の変更があるか。⚠ base は送信が成功した時しか更新されないので、**消した記録や直す前の値が、' +
      '画面に出ないまま次の同期成功まで平文で残る**',
    PP: '§2.1「部屋の中身（暗号化されていない形）」（⚠ 平文が2つあること・消した記録が残ることは監査 #2）',
  },
} as const
