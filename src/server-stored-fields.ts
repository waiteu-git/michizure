/**
 * 🔴 **サーバー（Durable Object）が部屋ごとに保存するもの＝事実の一覧。**
 *
 * これはプライバシーポリシー §2「サーバーが保存するもの」の**消費元**である。
 * ⇒ **ここを変えたら、PP §2 もビジネスハブと合わせて直すこと。** PP の文言は BH 所管なので、
 *    ここが固定するのは「実装が何を持つか」という事実までで、PP との対応は BH と詰める。
 *
 * なぜ要るか（2026-09-10）: 2026-09-07 に版の照合のため `rev`（書き込みのたびに1増える番号）を
 * 足した。これは**更新の回数そのもの**なのに、PP §2 は「更新の回数は数えていません（サーバーは
 * 持っていない）」と書いたままだった＝**持っているのに持っていないと書く過少申告**。
 * しかもその一文は 9/5 の監査で逆向きの誤りを直したばかりだった。実装を1行変えただけで、
 * 公開予定の PP が静かに嘘になった。**人が PP を洗い忘れても、ここが洗わせる。**
 *
 * 検査は2つ（片方だけでは足りない）:
 * - scripts/check-stored-fields.mjs … src/room.ts が書くキーが、ここに全部あるか（書く経路を問わず）
 * - test/stored-fields.test.ts   … 実際に一通り使った後の保存内容が、キーも欄もここと一致するか
 */
export const SERVER_STORED = {
  /** SQLite の `room` 表（key → JSON の値）に入るもの */
  room: {
    meta: {
      fields: ['createdAt', 'lastAccessAt', 'roomId', 'schemaVersion'],
      説明: '部屋のID・作成日時・最後に入室した日時・保存形式の版。自動削除の判定に使う',
      PP: '§2「部屋のID」「作成日時・最後に入室した日時」',
    },
    auth: {
      fields: ['authKeyHash', 'iterations', 'kdfVersion', 'salt'],
      説明: '合言葉から一方向に作った照合値のハッシュ・その計算に使う乱数・反復回数・文字の揃え方の版',
      PP: '§2「合言葉から作られた照合用の値」「鍵の作り方の設定」',
    },
    blob: {
      fields: ['blobVersion', 'ciphertext', 'iv'],
      説明: '利用者の入力を端末で暗号化したもの（サーバーは復号できない）と、その形式の版',
      PP: '§2「暗号化されたデータ」「暗号化されたデータの大きさ」',
    },
    rev: {
      fields: null, // 値は整数そのもの（JSON のオブジェクトではない）
      説明:
        '版の番号。作成で1、以後書き込みのたびに+1＝**その部屋が何回書き換えられたか（更新の回数）**。' +
        '中身は読めないが回数は分かる。部屋を消せば一緒に消える',
      PP: '⚠ 2026-09-10 時点で PP §2 に**未記載**（「更新の回数は数えていません」と書いてある＝要修正・BH へ報告済み）',
    },
    gate: {
      fields: ['blockedUntil', 'failures', 'lastFailureAt'],
      説明: '合言葉を続けて間違えた回数・直近に間違えた時刻・いつまで入室を止めるか',
      PP: '§2「合言葉を間違えた回数・直近に間違えた時刻・いつまで入室を止めるかの時刻」',
    },
  },
  /**
   * 🔴 **KV には何も置かない。** `ctx.storage.put()` を使うとここに入り、SQL からは見えない。
   * 使い始めたら、ここへ宣言を足し、PP §2 も直すこと。（alarm は list() に出ないので対象外）
   */
  kv: [] as string[],
} as const

/**
 * 保存はしないが、接続している間だけサーバーが持つもの（参考・検査の対象外）。
 * ⚠ PP に書くかは BH の判断。事実として残しておく。
 */
export const SERVER_HELD_WHILE_CONNECTED = {
  clientId: 'WebSocket の接続に括り付ける、端末のタブごとの乱数。書いた本人へ中継し返さないために使う。接続が切れれば消える',
}
